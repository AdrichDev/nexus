{
  "_comment": "Copia este archivo como mcp_servers.json y activa lo que quieras. Necesitas Node.js para los servidores npx. Estos son los MISMOS tipos de conector que usa Cowork.",
  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "C:\\ruta\\a\\tu\\carpeta"],
    "enabled": false
  },
  "github": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env_hint": "define GITHUB_PERSONAL_ACCESS_TOKEN en tu .env",
    "enabled": false
  },
  "brave-search": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-brave-search"],
    "env_hint": "define BRAVE_API_KEY en tu .env",
    "enabled": false
  }
}
